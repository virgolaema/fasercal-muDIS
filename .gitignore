# personal machine-specific paths
json/ev.json
# python
__pycache__/
*.pyc
# logs and generated outputs (bulk data + PDFs live on EOS)
logs/*.log
output/*.pdf
*.npz
# the compiled note IS tracked - it is the review deliverable
!note/fasercal_muondis.pdf

# LaTeX build products (the PDF itself is tracked on purpose)
note/*.aux
note/*.log
note/*.out
note/*.toc
